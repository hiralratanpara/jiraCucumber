{
  "scripts": {
    "cypress": "cypress open --env brand=we",
    "cypress:cli": "cypress run --env brand=we",
    "cypress:chrome": "cypress run --browser chrome",
    "cypress:firefox": "cypress run --browser firefox",
    "cypress:edge": "cypress run --browser edge"
  },
  "devDependencies": {
    "cypress": "^5.6.0"
  }
}
